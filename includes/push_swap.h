#define PUSH_SWAP_H
# define PUSH_SWAP_H

#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>

typedef struct node
{
  struct node *prev;
  int nbr;
  struct node *next;
};

int main(int ac, char **av)
{

}